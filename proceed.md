Paste this into Opus along with the plan document:

I have a finalized implementation plan attached. Your only job is to generate every file completely. Do not re-research. Do not question decisions. Do not summarize. Just build it.
Answer the open questions as follows before starting: Use tree-sitter-cpp for C++ (not libclang — Docker image size matters for the demo). Use Node.js subprocess with acorn for JavaScript AST (authentic ESTree, not pyjsparser). Use Redis for caching constraint check results to avoid re-analyzing identical code within 2 second windows.
Go file by file in exactly this order. Every file must be written in full — no placeholders, no "// TODO", no "same as before", no truncation. After every 4 files ask "continue?" before proceeding.
Order: docker-compose.yml, backend/Dockerfile, frontend/Dockerfile, frontend/nginx.conf, backend/requirements.txt, backend/main.py, backend/database.py, backend/models.py, backend/routers/auth.py, backend/routers/admin.py, backend/routers/problems.py, backend/routers/constraints.py, backend/routers/submit.py, backend/ast_engine/init.py, backend/ast_engine/python_analyzer.py, backend/ast_engine/java_analyzer.py, backend/ast_engine/c_analyzer.py, backend/ast_engine/cpp_analyzer.py, backend/ast_engine/js_analyzer.py, backend/ast_engine/js_ast_worker.js, backend/ast_engine/engine.py, backend/executor/docker_runner.py, backend/seed_problems.py, frontend/css/style.css, frontend/index.html, frontend/user.html, frontend/solve.html, frontend/admin.html, frontend/admin_dashboard.html, frontend/admin_edit.html, frontend/js/auth.js, frontend/js/user.js, frontend/js/solve.js, frontend/js/admin.js, frontend/js/admin_edit.js
Critical implementation rules to follow throughout:
The editable_ranges system is the core architecture. Never use top_code or bottom_code anywhere. Every template stores template_code as a full string and editable_ranges as a JSON array of objects with startLine and endLine. The constrained-editor-plugin from CDN https://unpkg.com/constrained-editor-plugin/dist/constrainedEditorPlugin.js enforces editable zones in Monaco. When language changes in the user editor, tear down all existing constraints before re-applying new ones so there is zero bleed between languages. If editable_ranges is empty, make the entire editor read-only and show a warning banner.
The AST engine must use proper AST parsing for all 5 languages — no keyword scanning anywhere. Python uses the built-in ast module. Java uses javalang. C uses pycparser. C++ uses tree-sitter with tree-sitter-cpp grammar. JavaScript uses acorn via a Node.js subprocess that writes results as JSON to stdout. All 5 analyzers must detect: while loops, for loops, do-while loops, if/else conditionals, switch statements, functions, recursion (function calling itself by name), classes, inheritance, exception handling, and data structures (arrays, linked lists, stacks, queues). C gets a comment explaining exception handling is skipped because C has no native try/catch. After getting violations, filter them so only violations whose line numbers fall within editable_ranges are returned to the user. Violations in locked zones are silently discarded. Line number indexing must be consistent between the analyzer output and editable_ranges — verify this explicitly in engine.py.
The Docker execution engine uses the Docker SDK for Python to spawn sibling containers by mounting the host Docker socket. Every container runs with --network none --memory=256m --cap-drop=ALL --pids-limit=50 and a hard 5 second timeout. All containers are removed after execution with no leaks. Per test case: pipe input to stdin, capture stdout, compare with expected output, return passed/failed with actual vs expected.
The admin editor in admin_edit.html must have a Monaco instance per language tab. Admin writes the full template code. Selecting lines and clicking "Mark as Editable" applies a green left border and subtle green background using deltaDecorations and stores the range. Clicking "Mark as Locked" removes the decoration and removes the range. When admin first pastes code, auto-detect the function body and pre-highlight it as editable. On save, serialize all highlighted ranges to editable_ranges JSON. Warn if zero ranges are defined.
The dark theme uses background #1a1a2e, accent #e94560, surface #0f3460, glassmorphism cards with backdrop-filter blur. Monaco locked lines use a darker background. Editable lines use a lighter background with green left border. Violation lines use red glow via box-shadow: 0 0 8px rgba(233,69,96,0.6).
Hardcoded credentials: admin/admin123 and user/user123. The entire project must work with a single docker compose up on any machine with Docker installed. Start with file 1 now.