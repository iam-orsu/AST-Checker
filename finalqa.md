Paste this into Opus with all the code:

---

You are not doing a code review. You are mentally executing this entire system from cold start to user submission, line by line, tracing every variable, every function call, every network request, every subprocess, every Docker API call. You are looking for the exact moment things break. Not categories of bugs. Exact lines. Exact reasons. Then you fix all of them.

Start from `docker compose up`. Mentally execute what happens in order. The Docker daemon reads docker-compose.yml. Each service builds. Trace the backend Dockerfile line by line — does every apt-get install succeed, is nodejs actually available in the apt registry for python:3.11-slim, does npm install acorn -g succeed inside that image, does pip install every package in requirements.txt without version conflicts, specifically does javalang install cleanly, does tree-sitter and the tree-sitter-cpp grammar install and compile without needing a C compiler that may not be in the slim image. Trace the frontend Dockerfile — does nginx start, does nginx.conf correctly proxy /api/ to backend:8000 using the Docker internal network hostname. Now all containers are up. Backend runs startup event — trace init_db() line by line, every CREATE TABLE statement, does it use IF NOT EXISTS everywhere, trace seed_db() — does it check if data already exists before inserting, what happens on the second `docker compose up` when data already exists. Does the seed function crash, duplicate, or skip correctly.

Now mentally open the browser and go to the login page. Trace auth.js — the form submits, fetch hits /api/login, trace auth.py — does it correctly compare credentials, what exactly does it return, is it a JWT or a session token, where is the secret key, is it hardcoded and will it survive container restart, trace what auth.js does with the response — does it store the token correctly in localStorage, does it read the role field correctly, does it redirect to the right page. Now what happens if someone navigates directly to admin_dashboard.html without logging in — trace exactly what check runs, where it runs, does it actually block the page or just show a flash before redirecting.

Now trace the admin creating a problem. Admin opens admin_edit.html. Trace admin_edit.js — Monaco loads via AMD loader, trace the exact CDN URLs used, will they resolve, does the AMD loader callback fire correctly, does initTemplateEditor() get called for all 5 language tabs or only the active one at load time. Admin switches to Python tab and pastes code. Trace the paste event handler — does it fire, does auto-detect function body actually parse the pasted code to find the function body or does it just guess line numbers, what algorithm detects the function body for Python vs Java vs C vs C++ vs JavaScript — are all 5 handled or just one. Admin selects lines 4 to 5 and clicks Mark as Editable. Trace exactly what happens — how does the code read the current selection from Monaco, is it editor.getSelection() or something else, are the line numbers 1-indexed from Monaco, trace how the range gets stored in memory, trace the deltaDecorations call — is the decoration ID stored so it can be removed later when Mark as Locked is clicked. Admin clicks save. Trace saveProblem() — how does it collect editable_ranges from the stored ranges in memory, does it produce [{startLine: 4, endLine: 5}] with 1-indexed lines, trace the fetch to POST /api/admin/problems, trace admin.py on the backend — does it correctly insert into problem_templates with template_code and editable_ranges as a JSON string, does it handle the test cases array correctly, does it return the new problem ID.

Now trace the user flow. User opens user.html. Trace user.js — fetch /api/problems, trace problems.py — does the SQL query return all expected fields, are topic_tags parsed from JSON string back to array before sending, does the response match what user.js expects. User clicks a problem card and navigates to solve.html?id=1. Trace solve.js initialization — Monaco AMD loader fires, constrained-editor-plugin CDN loads, trace the exact order these two scripts load and whether the plugin is guaranteed to be available before Monaco is used. Fetch /api/problems/1/template/python — trace problems.py — does it query problem_templates correctly, does it parse editable_ranges from JSON string to array before returning, what does it return if no template exists for that language. Back in solve.js — trace how the template is loaded into Monaco, trace how constrainedInstance is initialized, trace addRestrictionsTo — what exact format does it expect for ranges, is it [{range: [startLine, startCol, endLine, endCol]}] or [{range: {startLineNumber, startColumn, endLineNumber, endColumn}}] — check the plugin documentation format against what the code actually passes. Does it use 1-indexed or 0-indexed line numbers and does that match what came from the database. Trace what happens when user tries to type in a locked line — does the plugin block it, does it throw an error that crashes solve.js.

Now trace the live constraint check. User types in the editable zone. Debounce timer fires after 1.5 seconds. Trace the fetch to /api/check-constraints — what exactly is sent in the request body, is it the full template_code with user edits applied or just the user's typed code, trace constraints.py on the backend — does it correctly reconstruct the full code, does it pass editable_ranges to engine.py, trace engine.py dispatch to python_analyzer.py — trace ast.parse() on the full code, if the code is syntactically incomplete because the user is mid-typing does ast.parse() throw a SyntaxError, is that caught, what is returned to the frontend when parsing fails. If a violation is found on line 5 — trace the editable_ranges filter in engine.py — does it correctly check if 5 is between startLine and endLine inclusive, are these 1-indexed in both the analyzer output and the stored ranges. Trace the response back to solve.js — does it correctly call deltaDecorations with the violation line, does the decoration use the correct line number format Monaco expects (1-indexed), does the CSS class for red glow actually exist in style.css and is it a Monaco-compatible class applied via the decorations options object.

Now trace the submit flow. User clicks Submit. Trace solve.js — does it send the full assembled code or just the editable portion, trace submit.py — it calls engine.py for AST check first, if violations exist trace the exact response format and whether solve.js correctly reads it to show the error. If AST passes, trace docker_runner.py — it calls docker.from_env() — will this succeed when the Docker socket is mounted, does it handle the case where the socket is not mounted and give a clear error. It tries to run python:3.11-slim — is this image already pulled or does it pull on first run causing a timeout, does it correctly set network_disabled=True, mem_limit, cap_drop, pids_limit. Trace how the code file is passed to the container — is it written to a temp directory and mounted as a volume or passed as an environment variable or baked into a command string — if it is a mounted volume trace the exact path and whether the container can read it. Trace stdin piping for test case input — does the container actually receive stdin correctly for all 5 languages. Trace the timeout — does container.wait(timeout=5) raise an exception when it times out, is that exception caught, is the container removed in a finally block or does it leak on timeout. Trace the output comparison — is stdout stripped of whitespace before comparing with expected output. Trace the response back to solve.js — does it correctly render passed/failed per test case with actual vs expected.

For every single point where the trace breaks, shows an incorrect assumption, finds a missing error handler, finds a format mismatch, finds a race condition, or finds a silent failure — show the exact broken code, explain the exact failure mode, write the complete fix. Fix every file that needs fixing in full. Start the trace now.